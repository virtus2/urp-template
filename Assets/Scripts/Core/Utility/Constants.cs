
using UnityEngine;

namespace Core
{
    public static class Constants
    {
        public static string kCameraTransitionVolumeCameraName = "{0} | CinemachineCamera (Named by script)";
        public static int kCameraDefaultPriority = 10;
    }
}
