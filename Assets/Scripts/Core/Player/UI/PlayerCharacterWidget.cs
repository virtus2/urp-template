using UnityEngine;

namespace Core.Player.UI
{
    /// <summary>
    /// 플레이어 캐릭터의 child로 붙어있는 World space canvas UI
    /// </summary>
    [RequireComponent (typeof (Canvas))]
    public class PlayerCharacterWidget : MonoBehaviour
    {
    }
}